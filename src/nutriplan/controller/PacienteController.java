package nutriplan.controller;

import nutriplan.model.Paciente;

import java.time.LocalDate;

public class PacienteController {

    /*public boolean cadastrarPaciente(String nome, String CPF, String sexo, LocalDate dataNascimento, double altura, double peso, String atividade, String objetivo) {

        if(nome != null && nome.length() >0 && sexo != null && sexo.length() >0 && dataNascimento != null && altura >0 && peso >0 && atividade != null && atividade.length() >0 && objetivo != null && objetivo.length()>0){
            Paciente paciente = new Paciente(nome,CPF,sexo,dataNascimento,altura,peso,atividade,objetivo);
        }
    }*/
}
